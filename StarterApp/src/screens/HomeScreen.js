import React from "react";
import { Text, StyleSheet, View } from "react-native";
const HomeScreen = () => {
  return <Text style={styles.text}>H</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 20
  }
});

export default HomeScreen;
